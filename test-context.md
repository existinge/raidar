# Active Project Context
We are building a PostgreSQL data dashboard with Model Context Protocol (MCP) server support, enabling agents to execute safe read-only queries and present Postgres stats natively.
